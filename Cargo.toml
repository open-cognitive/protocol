[package]
name = "open_cognitive_protocol"
version = "0.1.0"
edition = "2021"
description = "Zero-copy data structures for Open-Cognitive OS"

[dependencies]
# Serileştirme gerektirmeyen veri paketlemesi için
# Derive makrolarını aktif ettik
bytemuck = { version = "1.25.0", features = ["derive"] }